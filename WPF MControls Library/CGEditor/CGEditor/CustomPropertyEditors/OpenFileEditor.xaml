<UserControl x:Class="CGEditor.CustomPropertyEditors.OpenFileEditor"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             x:Name="_uc"
             d:DesignHeight="24" d:DesignWidth="333">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>

        <TextBox Text="{Binding Value, ElementName=_uc}" Grid.Column="0" BorderThickness="0" />
        <Button Click="Button_Click" Grid.Column="1 " FontSize="12" FontWeight="Bold" Content="..." />
    </Grid>
</UserControl>
